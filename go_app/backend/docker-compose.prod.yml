services:
  app:
    image: ghcr.io/org/repo/app:latest
    env_file:
      - .env
    volumes:
      - sqlite-data:/app/data
    ports:
      - "8080:8080"
    restart: unless-stopped
  
volumes:
  sqlite-data: