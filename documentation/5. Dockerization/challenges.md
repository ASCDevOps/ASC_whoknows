# Dockerization

Docker build crashes the VM, the build works locally on stronger hardware.
We will have to wait for GHCR, so we can pull an image instead of building an image on the VM, to avoid crashing the app.
